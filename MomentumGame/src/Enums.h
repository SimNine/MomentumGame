#pragma once

enum DEBUG {
	DEBUG_NONE = 0,
	DEBUG_MINIMAL,
	DEBUG_NORMAL,
	DEBUG_EXTRA
};

enum ANIMPARTICLE {
	ANIMPARTICLE_WHITEDOT = 0,
	ANIMPARTICLE_SELECTRING
};

enum ANCHOR {
	ANCHOR_NORTHWEST,
	ANCHOR_NORTH,
	ANCHOR_NORTHEAST,
	ANCHOR_EAST,
	ANCHOR_SOUTHEAST,
	ANCHOR_SOUTH,
	ANCHOR_SOUTHWEST,
	ANCHOR_WEST,
	ANCHOR_CENTER
};

enum FONT {
	FONT_NORMAL,
	FONT_BOLD
};