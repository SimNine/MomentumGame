#pragma once

enum DEBUG {
	DEBUG_NONE = 0,
	DEBUG_MINIMAL,
	DEBUG_NORMAL,
	DEBUG_EXTRA
};

enum BKGMODE {
	BKGMODE_STANDARD,
	BKGMODE_RAINBOW,
	BKGMODE_REDBLUE
};

enum ANIMPARTICLE {
	ANIMPARTICLE_WHITEDOT = 0,
	ANIMPARTICLE_SELECTRING
};

enum ANCHOR {
	ANCHOR_NORTHWEST,
	ANCHOR_NORTH,
	ANCHOR_NORTHEAST,
	ANCHOR_EAST,
	ANCHOR_SOUTHEAST,
	ANCHOR_SOUTH,
	ANCHOR_SOUTHWEST,
	ANCHOR_WEST,
	ANCHOR_CENTER
};

enum FONT {
	FONT_NORMAL,
	FONT_BOLD
};

enum ACHIEVEMENT {
	ACHIEVEMENT_PLACEHOLDER1,
	ACHIEVEMENT_PLACEHOLDER2,
	ACHIEVEMENT_PLACEHOLDER3,

	ACHIEVEMENT_NUM_ACHIEVEMENTS,

	ACHIEVEMENT_NONE
};